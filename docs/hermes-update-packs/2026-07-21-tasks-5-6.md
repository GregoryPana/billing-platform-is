Hermes Update Pack — Billing Collaboration Platform

Session metadata
- Agent: Claude Code
- Date range covered: 2026-07-21 (consolidated flush of 2 pending entries, 16:05–16:10)
- Branch: feature/entra-id-auth
- Latest commit SHA: e123ec1 — pushed to origin: no (no upstream configured for this branch; nothing in this session has been committed yet either — all listed files are currently working-tree changes)
- Working directory: c:\Users\gpanagary\.gemini\antigravity\scratch\billing
- Task summary: Closed Task 5's outstanding browser/responsive/console verification gap (found and fixed two real, pre-existing bugs in the process) and completed Task 6 (Billing run-level execution issue capture), per docs/plans/2026-07-21-revenue-protection-issue-control.md.

Files changed
- frontend/src/App.jsx: `RequireRole` allowed-list for `/cycles` and `/cycles/:cycle_id` now includes `finance_user` (was billing_user/system_admin only). Bug fix — see "Decisions" below.
- frontend/src/features/approvals/ApprovalsInboxPage.jsx: added a "Review Finance Issues in Cycle Workspace" button on pending `stage="test"` approvals, navigating to `/cycles/:cycle_id`. Closes the discoverability gap left by the route fix above.
- frontend/src/App.css: renamed the custom `.table` → `.data-table` and `.table-row` → `.data-row` component classes (kept `.table-head`, which does not collide). Bug fix — see "Decisions" below.
- frontend/src/features/admin/AdministrationPage.jsx, frontend/src/features/cycles/CyclesListPage.jsx, frontend/src/features/cycles/NotificationsStage.jsx, frontend/src/features/overview/OverviewPage.jsx: class name updated to match the `.data-table`/`.data-row` rename only — no behavioral change.
- frontend/src/features/issues/FinanceIssuePanel.jsx: class name updated to match the rename only — no behavioral change.
- frontend/src/features/cycles/ScriptsRunsStage.jsx: Task 6 — added a per-run "Log Execution Issue" action and a compact "N logged" indicator badge (opens a read-only `IssueActivityDialog`); also updated for the class rename.
- frontend/src/features/issues/ExecutionIssueDialog.jsx (new): create-only dialog for logging a Billing/Admin execution issue against a specific script run. No completion/reopen lifecycle by design (see Decisions).
- frontend/package.json, frontend/package-lock.json: added `@playwright/test` as a new devDependency (Gregory approved) plus a one-time `npx playwright install chromium` (browser binary, not tracked in git).
- backend/alembic/versions/6ab1c9b21c7b_seed_execution_issue_classifications.py (new): additive migration seeding 5 `context="execution_issue"` classifications (Script Execution Failure, Parameter or Configuration Issue, Environment or Access Issue, Unexpected Output, Other). No existing table/data changed.
- backend/tests/test_execution_issue_routes.py (new, 5 tests, written before the migration per TDD): classification seeding, "Other requires detail", Billing list/read, run-status independence from open execution issues, and confirmation that execution issues never count toward the Move-to-Live gate.
- .opencode/hermes-pending-updates.md: two new entries logged (Task 5 gap closure, Task 6).
- C:\Users\gpanagary\.claude\plans\shiny-brewing-mango.md (outside repo, living plan tracker): Phase 5 marked verification-gap CLOSED with full detail on the two bugs found; Phase 6 marked DONE (locally).

Pre-existing dirty/untracked files not touched
- None. The working tree was fully clean at the start of this session (confirmed via `git status` before any changes); every modified/untracked file listed above was touched as part of this session's work.

Commands run
- `npm install -D @playwright/test` + `npx playwright install chromium` (frontend/) — succeeded
- `docker-compose up -d` / `docker-compose down -v` (disposable Postgres, multiple cycles as the DB needed a clean reset between verification passes) — succeeded each time
- `alembic heads` / `alembic upgrade head` / `alembic downgrade -1` / `alembic upgrade head` / `alembic current` — full cycle verified clean on a fresh database; single head `6ab1c9b21c7b`
- `pytest -q` (backend, disposable Postgres) — final run: 57 passed, 0 failed
- `npm run lint` (frontend) — clean, final run
- `npm run build` (frontend) — clean, final run
- A custom Playwright script (not committed — scratch verification tool, deleted after use) driving Chromium against the disposable stack (backend :8001, frontend :5180) as Billing, Finance, and Admin — 15/15 scripted checks passed for Task 5's states, plus 2/2 for Task 6's execution-issue flow; zero browser console errors captured in any run
- `taskkill` to stop the disposable backend/frontend processes; `docker-compose down -v` for final teardown

Tests / verification
- test/check: `pytest -q` (backend, disposable docker-compose Postgres, port 5435, torn down after)
- result: passed — 57/57
- failures: none
- coverage: includes the 5 new Task 6 tests (test_execution_issue_routes.py) plus the full pre-existing suite (auth roles, billing issue routes/permissions, approval issue gate)
- verification gaps: none for Tasks 5–6. Not verified in this session (pre-existing, unrelated open items — see Risks): the production `alembic stamp head` step, and the local 3-role login smoke test against Gregory's real dev database.

Implementation summary
- Task 5 verification: installed Playwright (approved), stood up a fully disposable backend+frontend+Postgres stack on non-conflicting ports (Gregory's own dev server on :5173 was never touched or interrupted), and scripted a real headless-browser walk of the Test Approval stage as Billing, Finance, and Admin — covering empty/open/completed-resolved/completed-raised-in-error/reopened states, the disabled-Approved-while-open behavior, the KPI-exclusion note, and both desktop (1440px) and mobile (390px) widths.
- Found and fixed bug #1: `finance_user` had no route to reach `FinanceIssuePanel` at all. `/cycles/:cycle_id` (which renders it) was gated to billing/admin only, and Finance's actual entry point (`ApprovalsInboxPage` via `/approvals`) never linked into it. The entire Task 5 UI was unreachable by a real Finance user before this fix.
- Found and fixed bug #2: the custom `.table`/`.table-row` CSS classes collided with Tailwind's own built-in `table`/`table-row` display utilities. Tailwind's later-loaded utilities layer won the cascade, silently collapsing every data table app-wide (Cycles list, Approvals Inbox, Administration, Overview, Notifications — not just the new Finance panel) to shrink-to-fit sizing with concatenated, non-columnar header text. This had been present since before this session and was only catchable by an actual browser render.
- Task 6: confirmed Task 3's `create_issue` route already fully supported the `execution_issue` context (role check, mandatory `related_script_run_id`, classification cross-check) — no backend route changes needed. The actual gap was that no `execution_issue` classifications had ever been seeded, making the path unreachable from a real UI. Added the seed migration, the TDD test file, the `ExecutionIssueDialog.jsx` create-only form, and the per-run action/indicator in `ScriptsRunsStage.jsx`.

Phase / roadmap impact
- Phase 5 (Task 5, Finance review + Billing visibility UI): the documented "browser/responsive/console check OUTSTANDING" gap from the prior session is now CLOSED.
- Phase 6 (Task 6, Billing execution issues): DONE (locally), matching Phases 1–5's local-only status.
- Per the original task-by-task scoping instruction for this plan, work stopped after Task 6, pending Gregory's go-ahead before Task 7 (Finance post-live observations).

Deployment impact
- deployment performed: no
- deployment needed: yes, eventually — both the frontend bundle (App.jsx, App.css, 7 other JSX files, new dialog) and the backend (new migration) have changed
- staging impact: none — nothing pushed or deployed anywhere this session
- production impact: none — production database and config were never touched; all verification ran against a disposable, throwaway Postgres container
- CI/CD impact: none — `.github/workflows/ci.yml` was not touched this session
- rollback consideration: the new migration's `downgrade()` deletes the 5 seeded `execution_issue` classification rows; verified it correctly raises a foreign-key violation (refuses to run) if any real `billing_issues` row still references one — this is the correct, safe behavior, not a bug

Auth / security / data impact
- auth/session impact: none
- role/access impact: **yes** — widened `RequireRole` on `/cycles` and `/cycles/:cycle_id` to include `finance_user` (read access to the cycle workspace tabs). All write actions inside those routes remain independently gated at the component level (e.g., script generation still requires `billing_user`/`system_admin`; Finance-issue create/edit/complete/reopen still requires `finance_user`/`system_admin`) — this was a routing-layer bug fix, not a new capability grant, and does not change the backend authorization matrix from Task 3/4 at all
- database/schema impact: one new additive migration (`6ab1c9b21c7b`) — 5 new classification rows, no changes to any existing table or column
- migration impact: `alembic heads`/`current` still show exactly one head; the pre-existing hard blocker in docs/DEPLOYMENT_SAFETY.md is unchanged and still outstanding — **production has still not been `alembic stamp head`'d**, and that must happen before this or any prior migration reaches production
- data safety impact: no destructive operations performed anywhere; all testing used a disposable, deleted-after Postgres container
- secret/env impact: none — verification used a fake, session-only `JWT_SECRET` and a disposable database; `backend/.env.local`'s real values (including the n8n webhook URL) were never read, printed, or exercised. A temporary CORS entry (`http://localhost:5180`) was added to `backend/app/main.py` to allow the disposable frontend port to reach the disposable backend during verification, and was reverted before this pack was written — `git diff` on that file is now empty
- security risks: none new

Documentation / handover impact
- docs changed: C:\Users\gpanagary\.claude\plans\shiny-brewing-mango.md (Phase 5 and Phase 6 status updated with full detail)
- EXIT/handover impact: none this session
- what Hermes should record: both bugs found this session (the Finance routing gap and the Tailwind class-name collision) as durable project knowledge — the second one in particular is a pattern risk (any future custom CSS class named after a Tailwind core utility value will silently lose the cascade to the utilities layer) worth a standing convention note

Decisions
- Confirmed: Gregory approved installing `@playwright/test` + Chromium as a new frontend devDependency for this session's verification.
- New decisions required/made: routing fix scoped narrowly — `finance_user` gets route access to `/cycles`/`/cycles/:cycle_id` plus one explicit link from the Approvals Inbox, not a full "Billing Cycles" sidebar nav item (Finance's primary entry point remains the Approvals Inbox, matching the design doc's "not a general ticket queue" intent). Execution issues are intentionally create+view only with no completion/reopen lifecycle, matching the design doc's "run status is the sole readiness determinant" rule.

Risks / open questions
- new risks: none — both issues found this session were pre-existing and are now fixed and verified.
- resolved risks: Task 5's outstanding Frontend Design Quality Gate gap is closed; the Finance-unreachable-UI bug and the app-wide table rendering bug are both fixed and reverified end-to-end.
- changed risks: none.
- open questions: none new this session.
- blockers (all pre-existing, unrelated to this session's work, still outstanding): (1) production `alembic stamp head` has still not been run — documented as a hard blocker in docs/DEPLOYMENT_SAFETY.md before this branch can ever be merged to `main`/deployed; (2) the local 3-role login smoke test against Gregory's real dev database (open since Phase 0) has still not been run.

Suggested Hermes vault updates
- project overview: Tasks 5 and 6 of the Revenue Protection Issue Control plan are now done locally (6 of 9 tasks complete); Task 5's verification gap is closed.
- technical architecture: record the `.data-table`/`.data-row` naming convention — custom component CSS classes in this codebase must avoid names that collide with Tailwind's core utility values (`table`, `table-row`, `table-cell`, `grid`, `flex`, `hidden`, `block`, etc.), since the utilities layer loads after `@layer components` and silently wins the cascade.
- deployment/CI-CD: still blocked on the production `alembic stamp head` one-time step before this branch merges to `main` or deploys — unchanged from every prior session's note.
- risks/open questions: two real bugs found and fixed this session (Finance routing gap, Tailwind class collision); the production stamp-head step and the 3-role smoke test remain the only two outstanding manual items from earlier phases.
- decision log: `finance_user` now has route-level (read) access to the cycle workspace; execution issues have no completion/reopen lifecycle by design.
- process/skills: this session is a strong example of why the Frontend Design Quality Gate's actual-browser-render requirement matters — `npm run lint`, `npm run build`, and curl-based API checks all passed cleanly in the prior session while two real, user-facing bugs (one blocking Finance's entire primary workflow) sat undetected.

Next recommended task
- Gregory's explicit go-ahead needed before Task 7 (Finance post-live observations), per this plan's task-by-task scoping instruction.
- Alternatively/in parallel: clear the two long-standing manual blockers (production `alembic stamp head`, 3-role local login smoke test) so they stop accumulating across sessions.
