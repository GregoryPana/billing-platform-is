# Hermes Pending Updates

## 2026-07-28 — Dropped unused signup_requests table; collapsed Issue Reporting metric metadata behind a details toggle
- branch/commit: `feature/cycle-usage-month-derivation` @ `93e60fb` (signup_requests migration+docs, pushed: no) + uncommitted `BillingIssueReportingPage.jsx` change (reporting UI cleanup, not yet committed pending Gregory's review)
- files: backend - `backend/alembic/versions/f3a9c1d7e2b4_drop_signup_requests_table.py` (new, drops `signup_requests`, down_revision `6ab1c9b21c7b`), `backend/app/db/init_db.py` (removed stale comment about leaving the table in place); docs - `README.md`, `architecture/blueprint.md`, `project.md` (dropped `signup_requests` from table lists), `docs/entra-id-integration-plan.md` (replaced the forward-looking "Signup requests" recommendation section with a note that it's done); frontend - `frontend/src/features/reporting/BillingIssueReportingPage.jsx` (`MetricSection`'s always-visible Source/Filter-scope block moved into a collapsed `<details className="panel-details">`, reusing an existing but previously-unused CSS pattern)
- verification: migration upgrade/downgrade/upgrade verified clean against a disposable Postgres (single head); full backend suite 107/107 passing against a separate disposable Postgres with the new migration applied; frontend `eslint` + `npm run build` clean; browser-verified via a disposable stack (backend :8098, frontend :5198, temporary CORS entry added and reverted - `git diff` on `main.py` is empty) logged in as the break-glass admin - all 6 Issue Reporting metric cards render collapsed by default with a working "Data details" expand/collapse, zero console errors
- flags: **destructive DB command not yet run anywhere** - the migration is committed to the branch only; it will apply automatically via CI's `alembic upgrade head` on merge to `main` + deploy, per Gregory's explicit choice not to apply it manually first | decision made (kept `source`/`filter_scope` data in the API response, only changed what's rendered by default - Task 8's audit-traceability requirement is preserved, just no longer forced onto every card as visible copy) | **incidental: while tearing down local verification processes, ran `taskkill /F /IM node.exe` which kills all node processes system-wide, not scoped to the disposable dev server - flagged to Gregory in-conversation since it could have affected any of his own running node processes at the time** | no auth/security impact beyond removing dead schema already superseded by Entra-only auth

## Flushed 2026-07-27 (billing month derived from confirmed usage month + script-date bug fixed and deployed via PR #15; deploy-pipeline incident hotfixed; local login/user-creation retired for Entra-only auth; dialog overflow bug fixed; real root cause of all prior Entra 401s found and fixed; Entra audience-format bug fixed — consolidated Hermes Update Pack delivered in-conversation)

## 2026-07-27 (latest) — Billing month is now derived from a confirmed usage month; script generation dates fixed to use it instead of the real current date
- branch/commit: `feature/cycle-usage-month-derivation` (merged to `main` via PR #15, commit `7c43f49`)
- files: backend - `app/utils/datetime_utils.py` (new `next_month_str()` helper), `app/schemas/cycles.py` (`BillingCycleCreate` drops `billing_month` — only `usage_month` is user-supplied now), `app/api/routes/cycles.py` (`create_cycle` derives `billing_month = next_month_str(usage_month)` server-side), `app/services/command_service.py` (`_default_preparation_params`/`_default_printing_params` now take `usage_month` and compute P3/P4 dates and the printing date range from it, replacing the prior `utc_plus_4_now()`/`date.today()` calls), `app/api/routes/scripts.py` (`generate_scripts` fetches the `BillingCycle` and passes `cycle.usage_month` into `generate_parameters`); frontend - `CyclesListPage.jsx` (Create Cycle form is now a single "Usage month" picker, auto-suggested to the current calendar month, editable/confirmable, plus a read-only derived "Billing month" preview field), `ScriptsRunsStage.jsx` (its own parameter-preview `cycle_month` now reads `cycle.usage_month` directly instead of `cycle.billing_month || cycle.usage_month`, matching the backend's source of truth)
- verification: 105/105 backend tests passing against local docker `billing_postgres` (:5435, started this session); `eslint` on all changed frontend files clean; CI `test` + `deploy` jobs both green post-merge (`gh run watch` confirmed: migrations ran, `billing-api` restarted, health check passed)
- flags: **real pre-existing bug fixed, not introduced this session**: script generation's date parameters (P3/P4 and the printing script's date range) were computed from the *actual current date* at generation time, not from the cycle's usage month — generating scripts for an already-past usage month (e.g. a June cycle run in late July) silently produced wrong dates. No known production incident traced to this yet, but it was live in every prior deploy. | decision made, confirmed with Gregory: no backdated/late cycles exist in this business's process, so `billing_month` is always exactly `usage_month + 1` with no exceptions — collapsed to a derived value rather than two independently-editable fields, closing the class of error where they could be set inconsistently | decision made: kept the `billing_month` column/read-schema field as-is (not dropped) since `issue_reporting_service.py` and `approvals.py` still filter/label by it — only the *write path* changed | auto-suggested default for the usage-month picker (current calendar month) was Gregory's explicit choice, confirmed via AskUserQuestion | no schema/migration change — `billing_month` was already a plain string column, just no longer client-settable

## 2026-07-27 (post-merge incident) — feature/entra-only-auth (PR #13) broke the deploy pipeline; hotfixed
- branch/commit: `fix/settings-ignore-extra-env-vars` (committed off `main`, PR pending)
- files: `backend/app/config.py` (`Settings.model_config` now sets `extra="ignore"`)
- verification: after PR #13 merged, its CI run failed at the "Run database migrations" step (`gh run view` confirmed: `test` job passed, `deploy` job failed exactly there). Root cause: `alembic upgrade head` imports `app.config`, and pydantic-settings rejects unknown env vars by default - PR #13 removed `n8n_signup_webhook_url`/`n8n_signup_approve_webhook_url` from `Settings`, but `.github/workflows/ci.yml` still unconditionally writes `N8N_SIGNUP_WEBHOOK_URL`/`N8N_SIGNUP_APPROVE_WEBHOOK_URL` into production's `.env` on every deploy (not touched, since that's a CI/CD file requiring separate approval) - so the now-undeclared env vars crashed `Settings()` construction. **No outage occurred**: the failure happened before the "Restart billing API" step, so systemd's `billing-api.service` kept running the previous (PR #11) deploy the entire time, confirmed via `systemctl status` showing a single stable PID with no restart since 07:29 UTC. Reproduced the exact crash locally with the same env vars set and confirmed `extra="ignore"` fixes it; 107/107 backend tests still passing against a disposable Postgres (torn down after).
- flags: **this is the direct cause of the previous entry's "not yet deployed/verified" state** - PR #13 never actually finished deploying, so none of its behavior (break-glass-only seeding, retired signup routes, etc.) is live yet | decision made: fixed the app to tolerate stray/legacy env vars generally (`extra="ignore"`) rather than only patching `ci.yml` to stop writing the two specific retired vars - this closes the whole class of "app code and ci.yml env-var lists must always be kept in perfect sync or startup crashes" risk, not just this one instance | discovered mid-flow while attempting a full test-data reset (Gregory confirmed all current billing_cycles/etc. data is pre-launch test data, to be cleared before a final test and go-live) - that reset is still pending, blocked on this fix landing and a successful redeploy first

## 2026-07-27 (final) — Local login/user-creation retired; Entra is now the sole routine auth path
- branch/commit: `feature/entra-only-auth` (committed off `main`, PR pending)
- files: backend - `app/api/routes/auth.py` (removed `/signup`, `/requests`, `/requests/{id}/approve`, `/requests/{id}/reject`; `/login` and `/me` unchanged), `app/api/routes/users.py` (removed `POST /users/` create; `_display_role()` fallback kept from the earlier viewer-role fix), `app/schemas/auth.py` + `app/schemas/users.py` (dropped `SignupRequestCreate`/`SignupRequestRead`/`SignupApproval`/`UserCreate`), `app/models/signup_request.py` (deleted - table itself left in place, not dropped, since that's a separate migration requiring its own approval), `app/models/__init__.py`, `app/config.py` (dropped `n8n_signup_webhook_url`/`n8n_signup_approve_webhook_url`), `app/db/init_db.py` (seeds only one break-glass local `system_admin` now, not billing/finance locals; removed a dead schema-safety-net block for `signup_requests` that would have crashed a truly fresh non-Alembic bootstrap now that the model is gone - caught during browser verification, not left in); frontend - `LoginPage.jsx` (signup form/toggle removed), `AdministrationPage.jsx` (removed "Access Requests" tab and "Create User" panel; Edit form drops the Role field with new copy explaining Entra controls real access, not this row), `AppDataContext.jsx` (dropped `signup_requests` state/fetch), `nav.js` (stale "access requests" copy fixed); tests - `conftest.py` (`auth_headers` now uses a `dependency_overrides[get_current_actor]` fake actor instead of a real login, since only one local account exists in prod for it to log in as; new `test_actor_id` fixture for FK-backed real user ids; `_seed_test_role_users()` backs the fake actors with real rows since `billing_issue_activity.actor_id` is a NOT NULL FK to `users.id`), 6 test files updated to match (`test_auth_roles.py` drops the now-nonexistent billing/finance local-login tests; `test_entra_dual_auth.py`'s local-login test now uses the break-glass admin directly and its role-gated-route test target moved from the removed `/auth/requests` to `/users/`; 5 files swapped a real-login-just-to-get-a-user-id pattern for `test_actor_id`)
- verification: 107/107 backend tests passing against a disposable Postgres (torn down after); `npm run build` + `npm run lint` both clean; full browser pass via a disposable stack (backend :8099, frontend :5199, temporary CORS entry for :5199 added and reverted before finishing - `git diff` on `main.py` is empty) - login page shows no signup form, break-glass admin logs in successfully with zero console errors, Administration has no "Access Requests" tab, Users tab shows no "Create User" panel and the Edit form has no Role field
- flags: **major auth/security surface change** - local self-service signup and admin-created local accounts are both retired; Entra app-role group membership is now the only way to grant access, matching Gregory's explicit direction | **production action required, not done here**: production's `users` table already has rows, so `init_db()`'s seeding is a no-op there (it only seeds an empty table) - the break-glass admin does not exist yet in production and must be created manually after this deploys (command given directly to Gregory, password chosen and set by him, never disclosed to or generated by this session) | **production action required**: the 6 pre-existing local test rows (4 finance, 2 billing - confirmed by Gregory to be test data, not real users) still need deleting post-deploy, same explicit-approval pattern as the earlier `viewer`-role row | decision made: `signup_requests` table left in the database rather than dropped - dropping it is a schema migration, kept as a distinct, separately-approved follow-up rather than bundled here | decision made: kept `PATCH /users/{id}` (password reset / is_active toggle / name / email) rather than removing all user-management - only creation and role-editing were retired, since role for Entra rows is never read from this table anyway | rollback: if Entra becomes unavailable, the break-glass admin (once created) is the only fallback path; local billing_user/finance_user login no longer exists at all

## 2026-07-27 (later still) — Dialog forms overflowing their modal panel (unrelated UI bug, found while verifying Entra was fully working)
- branch/commit: `fix/dialog-form-grid-overflow` (committed off `main`, PR pending)
- files: `frontend/src/components/ui/dialog.jsx` (panel now tagged with a `dialog-panel` class), `frontend/src/App.css` (scoped override: `.dialog-panel .form-grid { grid-template-columns: minmax(0, 1fr); }`)
- verification: Gregory reported the Log Execution Issue dialog's fields and dropdown visually exceeding the modal's bounds. Root cause: `.form-grid` is a real 3-column grid (`repeat(3, minmax(220px, 1fr))`) used correctly on wide pages (e.g. Administration's user form), but `ExecutionIssueDialog`/`IssueFormDialog`/`IssueActivityDialog` reuse it inside narrow `max-w-lg` (~512px) dialogs where every field is `.full` (spans all 3 columns) — the grid still reserves a 660px minimum regardless, overflowing the ~464px available and pushing the native `<select>` past the modal edge. `npm run build` + `npm run lint` both clean; rendered the exact compiled CSS against the same markup with a disposable Playwright script (not committed) — form content measured 462px against a 464px content box (fits) versus overflowing before the fix.
- flags: no auth/security/data impact — pure CSS/markup fix | scoped to a `.dialog-panel` class rather than changing `.form-grid` globally, so the legitimate wide-page 3-column layouts (Administration, etc.) are untouched | fixes three dialogs at once since all three go through the shared `Dialog` component

## 2026-07-27 (later) — Real root cause found: production venv missing `cryptography`, masked by generic 401s all along
- branch/commit: `fix/entra-missing-crypto-dependency` (merged to `main` via PR #10)
- files: `backend/requirements.txt` (added `cryptography==43.0.3`, the RS256 backend PyJWT needs — was never listed, only `PyJWT==2.9.0`), `backend/tests/test_entra_auth_service.py` (+1 test: real RS256 sign/verify round trip using a freshly generated RSA key, not mocked)
- verification: after the audience fix (PR #9) deployed successfully (confirmed via `gh run view` — commit `8324145d`, `health_status=active`, restart at 05:16:27 UTC) Gregory still got a 401 on a *fresh interactive* Microsoft sign-in (not silent SSO this time). Replicated the exact production validation logic locally against Azure's live public JWKS for this tenant/app (real signature check, same audience/issuer lists as the deployed fix) and it passed cleanly — proving the token was valid and the audience/issuer code was correct. Had Gregory SSH to the box and confirm: service running the correct deployed code (`_entra_acceptable_audiences()` returned the right list interactively), but running the *exact* failing token through the real validation path server-side raised `jwt.exceptions.MissingCryptographyError: RS256 requires 'cryptography' to be installed` — caught by the broad `except jwt.PyJWTError` in `validate_entra_token()` and surfaced to the client as a generic `{"detail":"Invalid token"}` 401. 18/18 non-DB unit tests passing locally, including the new real-signature test (which would itself fail with `MissingCryptographyError` if this dependency gap recurred).
- flags: auth/security/data impact (adds a hard dependency required for RS256 verification to function at all — no behavior relaxation, the opposite: RS256 verification was silently non-functional in production this whole time) | **likely explains every prior Entra 401** including the earlier issuer and audience investigations — those root causes (v1/v2 issuer format, bare-client-ID audience) are real, confirmed via decoded token claims, and the code fixes for them are correct and worth keeping, but this missing dependency means no real Entra token could ever have passed signature verification in production until now, regardless of audience/issuer correctness | **test-coverage gap identified and partially closed:** every other test in `test_entra_auth_service.py` mocks `jwt.decode`/the signing key, so 105+/105 "passing" CI runs never caught that `cryptography` was absent from `requirements.txt` — new real-RS256 test closes this specific gap | **not yet deployed** — pushed to `fix/entra-missing-crypto-dependency`, needs merge to `main` + production redeploy, then a real end-to-end sign-in retest, before Phase A can be considered actually working | also identified (not yet fixed): `frontend/src/App.jsx`'s Entra bootstrap `catch` block silently swallows any post-redirect auth failure with no visible error — made every one of these bugs harder to diagnose from the browser alone; still worth a follow-up

## 2026-07-27 — Phase A second live-fire bug: Entra audience mismatch (bare client ID vs api:// URI) — merged
- branch/commit: `fix/entra-audience-format` (merged to `main` via PR #9, commit `283639c`)
- files: `backend/app/services/entra_auth_service.py` (`_entra_audience()` replaced with `_entra_acceptable_audiences()`, now accepts both `api://<client_id>` and the bare client-ID GUID in `jwt.decode(audience=...)`), `backend/tests/test_entra_auth_service.py` (+3 tests)
- verification: Gregory reproduced in browser DevTools — MSAL redirect completed silently (active SSO session, no login prompt shown), token acquired, but `GET /billing-api/auth/me` returned 401. Decoded the token payload: `ver: "2.0"`, `iss` correctly `login.microsoftonline.com/.../v2.0` (confirms the 2026-07-24 issuer fix holds in production), but `aud: "ff645f66-7fab-4907-8ed0-3f232af516f8"` (bare client ID) instead of the expected `api://ff645f66-7fab-4907-8ed0-3f232af516f8`. Root cause: Azure AD returns the bare client-ID GUID as audience for this SPA-calls-its-own-API app registration shape, regardless of the App ID URI used to request the scope. Deploy confirmed successful (CI run, commit `8324145d`, `health_status=active`).
- flags: auth/security/data impact (backend now accepts a second audience string as valid — deliberately narrow, tied to this app's own client ID, not a general relaxation) | **this fix alone did not resolve sign-in** — see the entry above; a second, deeper bug (missing `cryptography` dependency) was masking the real end-to-end result | pattern repeats the 2026-07-24 issuer bug almost exactly (self-referencing SPA-calls-own-API app registrations produce non-obvious token claim formats); worth a second `06_ENTRA_ID_INTEGRATION_GUIDE.md` troubleshooting entry for the audience case, same as it got for issuer

## Flushed 2026-07-24 (production deploy pipeline restored, Task 9 local blockers closed, Entra Phase A deployed live, token-version bug found and fixed, org guide updated — consolidated Hermes Update Pack delivered in-conversation, all now committed/merged: PRs #4, #5, #6, #7)

## Flushed 2026-07-23 (Tasks 7, 8, and Task 9 first slice — consolidated Hermes Update Pack delivered in-conversation, all now committed: 2744cdf, e9d5f80, aea0801)

## 2026-07-22 (evening) — Task 9 of Revenue Protection Issue Control plan (Entra enforcement and safe rollout — code/test/doc slice)
- branch/commit: feature/entra-id-auth @ aea0801 (committed 2026-07-23, pushed to origin: no)
- files: backend/tests/test_entra_auth_service.py (new, 14 tests), backend/tests/test_entra_dual_auth.py (new, 6 tests), EXIT.md (new, repo root), docs/entra-id-integration-plan.md (modified — added "Implementation status (2026-07-22, Task 9)" section)
- verification: full backend suite 102/102 passing (82 prior + 20 new) against a disposable Postgres container on a throwaway port (5441, torn down after); npm run lint + npm run build both clean (no frontend code changed this slice); alembic heads unchanged at single head 6ab1c9b21c7b (no migration needed — no schema change)
- flags: auth/security/data impact (new test coverage only, no behavior change — closes the "no automated Entra auth test exists yet" gap called out in commit 8f6d8db) | decision made (did not modify 06_ENTRA_ID_INTEGRATION_GUIDE.md — it's a generic org-wide onboarding guide referenced by other apps, nothing in it was wrong for this app) | decision made (did not modify .github/workflows/ci.yml — existing deploy job already wires every ENTRA_*/VITE_ENTRA_* secret with safe false defaults and already produces the uptime+alembic-current+role-check deploy proof the plan requires; the phased pilot rollout is an Entra-side group/role-assignment decision, not a CI/CD staging change, so none was needed, consistent with CLAUDE.md's "don't alter CI/CD without explicit approval") | new risk: none beyond the existing pre-merge production alembic-stamp requirement | **the actual production Entra rollout (Phase A-D) has NOT started** — requires a real Azure AD app registration (none exists yet), the two long-standing local blockers (production alembic stamp, 3-role login smoke test) resolved, and Gregory's explicit approval at each production step

## 2026-07-22 (later) — Task 8 of Revenue Protection Issue Control plan (cycle-quality reporting)
- branch/commit: feature/entra-id-auth @ e9d5f80 (committed 2026-07-22, pushed to origin: no)
- files: backend/app/api/routes/issue_reporting.py (new), backend/app/schemas/issue_reporting.py (new), backend/app/services/issue_reporting_service.py (new), backend/tests/test_issue_reporting.py (new, 13 tests, TDD-first), backend/app/api/router.py (modified — registered issue-reporting router), backend/app/services/issue_control_service.py (modified — `ensure_move_to_live_not_blocked` now takes actor_id/actor_type and records a `move_to_live_blocked` audit event on every block), backend/app/api/routes/approvals.py (modified — passes actor through to the gate), frontend/src/features/reporting/{reporting-api.js, BillingIssueReportingPage.jsx} (new), frontend/src/components/layout/nav.js (modified — new Finance/Admin-only "Issue Reporting" nav item), frontend/src/App.jsx (modified — new `/reporting` route, same role gate)
- verification: full backend suite 82/82 passed (69 prior + 13 new) against a disposable Postgres container on a throwaway port (5439, distinct from the shared dev container on 5435 to avoid any risk of wiping real data), torn down after; npm run lint + npm run build both clean; browser-verified with a scratch (uncommitted, deleted after use) Playwright script against a disposable stack (backend :8001, frontend :5180 with an explicit `VITE_API_URL` override since `.env`/`.env.local` point at :8000, Gregory's own :5173/:8000 never touched) — Finance desktop and mobile (390px) both render all six metrics with real seeded data, Admin can view, Billing has no sidebar entry and is redirected away from a direct `/reporting` URL, zero console errors in any role
- flags: auth/security/data impact (new: `ensure_move_to_live_not_blocked` signature change adds an audit-log write on every gate rejection — this is the only real data source available for the "cycles blocked by an open issue" metric, since blocked attempts were not previously persisted anywhere; verified via `test_cycles_blocked_by_open_issue` / `test_cycles_blocked_ignores_unblocked_approvals`, and the full approval-gate suite still passes unchanged) | decision made (reporting endpoint restricted to Finance/Admin only, matching the plan's "Finance/Admin decision-ready evidence" framing — Billing was not given read access to this page) | decision made (every metric response includes explicit `source`, `filter_scope`, `decision_supported`, and `is_empty` fields per the Task 8 acceptance criterion, rather than leaving that context only in code comments or docs) | decision made (no revenue-at-risk or financial values calculated anywhere in this endpoint, per plan scope) | temporary CORS entry (http://localhost:5180) added to backend/app/main.py for verification and reverted before finishing — git diff on that file is empty | disposable Postgres container used a non-default port (5439) and was fully removed (`docker rm`) after use, not left running | no new risk beyond the existing pre-merge production alembic-stamp requirement documented in docs/DEPLOYMENT_SAFETY.md

## 2026-07-22 09:05 — Task 7 of Revenue Protection Issue Control plan (Finance post-live observations)
- branch/commit: feature/entra-id-auth @ 2744cdf (committed 2026-07-22, pushed to origin: no)
- files: backend/tests/test_post_live_observation_permissions.py (new, 12 tests, TDD-first), frontend/src/features/issues/PostLiveObservationPanel.jsx (new), frontend/src/features/cycles/NotificationsStage.jsx (modified — panel wired in above the notification-guide content)
- verification: full backend suite 69/69 passed (57 prior + 12 new) against a disposable docker-compose Postgres (torn down after); npm run lint + npm run build both clean; browser-verified with a scratch (uncommitted, deleted after use) Playwright script against a disposable stack (backend :8001, frontend :5180, Gregory's own 5173 never touched) — Billing read-only with 0 create buttons and correct empty state, Finance create/complete works and 0 reopen buttons appear post-completion, Admin sees the create button, mobile 390px renders correctly, zero console errors in any role
- flags: auth/security/data impact: none new — backend already fully implemented the post_live_observation permission matrix from Task 3's original context-branching design (no issues.py changes needed); the new test file is dedicated documentation/coverage, not a behavior change | decision made (reused IssueFormDialog/IssueActivityDialog as-is rather than new components — IssueFormDialog already had post_live_observation copy pre-built from Task 5, and IssueActivityDialog's reopen gate already keys off context so passing test_approved={false} is safe/inert for this context) | temporary CORS entry (http://localhost:5180) added to backend/app/main.py for verification and reverted before finishing — git diff on that file is empty | no new risk beyond the existing pre-merge production alembic-stamp requirement documented in docs/DEPLOYMENT_SAFETY.md

## Flushed 2026-07-21 (third flush, 16:15 — Tasks 5 gap closure, 6) — see docs/hermes-update-packs/2026-07-21-tasks-5-6.md

## 2026-07-21 16:05 — Task 5 browser verification gap closed (two real bugs found and fixed)
- branch/commit: feature/entra-id-auth @ e123ec1 (uncommitted changes on top, pushed to origin: no)
- files: frontend/src/App.jsx (RequireRole allowed-list for /cycles and /cycles/:cycle_id now includes finance_user), frontend/src/features/approvals/ApprovalsInboxPage.jsx (added "Review Finance Issues in Cycle Workspace" link for stage="test" pending approvals), frontend/src/App.css (renamed colliding `.table`→`.data-table` and `.table-row`→`.data-row`), 8 JSX files updated to match the renamed classes (AdministrationPage, ApprovalsInboxPage, CyclesListPage, NotificationsStage, ScriptsRunsStage, FinanceIssuePanel, OverviewPage), frontend/package.json + package-lock.json (added @playwright/test as a new devDependency, approved by Gregory)
- verification: installed Playwright+Chromium and drove the app with a disposable docker-compose Postgres (alembic-migrated, torn down after) and disposable backend/frontend instances (ports 8001/5180, Gregory's own dev server on 5173 left untouched throughout). 15/15 scripted browser checks passed across Billing/Finance/Admin roles and all required states (empty, open, completed-resolved, completed-raised-in-error, reopened, disabled-Approved-while-open, mobile 390px width); zero browser console errors on any page
- flags: **two real, pre-existing bugs found and fixed, not introduced this session**: (1) `finance_user` had no route to reach `ApprovalStage.jsx`/`FinanceIssuePanel` at all — `/cycles/:cycle_id` was billing/admin-only and `ApprovalsInboxPage` (Finance's actual entry point) never linked into it, so the entire Task 5 Finance UI was unreachable by real Finance users since it was built; (2) the custom `.table`/`.table-row` CSS component classes collided with Tailwind's own built-in `table`/`table-row` display utilities, and the later-loaded utilities layer won the cascade, collapsing every data table in the app (not just Task 5's) to shrink-to-fit sizing with concatenated, non-columnar header text — this affected CyclesListPage, ApprovalsInboxPage, AdministrationPage, OverviewPage, and NotificationsStage too, and was never caught before because no prior session had actually rendered the app in a browser | decision made (routing fix scoped to /cycles and /cycles/:cycle_id only, not the nav sidebar item — Finance's primary entry point remains Approvals Inbox with an explicit link out, not a full "Billing Cycles" nav item) | no schema/auth-model impact, pure frontend routing + CSS naming fix

## 2026-07-21 16:10 — Task 6 of Revenue Protection Issue Control plan (Billing execution issue capture)
- branch/commit: feature/entra-id-auth @ e123ec1 (uncommitted changes on top, pushed to origin: no)
- files: backend/alembic/versions/6ab1c9b21c7b_seed_execution_issue_classifications.py (new — seeds 5 execution_issue classifications: Script Execution Failure, Parameter or Configuration Issue, Environment or Access Issue, Unexpected Output, Other), backend/tests/test_execution_issue_routes.py (new, 5 tests, TDD-first), frontend/src/features/issues/ExecutionIssueDialog.jsx (new — create-only dialog, no completion/reopen lifecycle by design since execution issues never block run readiness), frontend/src/features/cycles/ScriptsRunsStage.jsx (modified — added per-run "Log Execution Issue" action and a compact "N logged" indicator badge, clickable to a read-only IssueActivityDialog view)
- verification: full backend suite 57/57 passed against a disposable docker-compose Postgres (torn down after); alembic heads/upgrade/downgrade/upgrade cycle verified on a clean database (downgrade correctly fails with a FK violation if execution_issue classifications are still referenced by real issue rows — confirmed intentional, not a bug); frontend npm run lint + npm run build both clean; browser-verified end-to-end (see Task 5 entry above for the same session) — generated scripts, marked a run executed, logged an execution issue against it, confirmed the "1 logged" badge and dialog render correctly, zero console errors
- flags: decision made (backend for Task 3's create-execution-issue path already existed; only the missing classification seed data and the list/frontend wiring were actually needed for Task 6) | decision made (execution issues are create+view only, no complete/reopen lifecycle in the UI, matching the design doc's "run status remains the sole readiness determinant" rule) | data impact: additive-only migration (5 new classification rows, context="execution_issue"), no changes to existing tables; alembic heads/current unchanged in head count (single head `6ab1c9b21c7b`) | no new risk beyond the existing pre-merge production alembic-stamp requirement documented in docs/DEPLOYMENT_SAFETY.md

## Flushed 2026-07-21 (second flush, 11:50 — Tasks 3, 4, 5)

## 2026-07-21 11:19 — Task 3 of Revenue Protection Issue Control plan (authorization + issue API lifecycle)
- branch/commit: feature/entra-id-auth @ d9d8d90 (pushed to origin: no)
- files: backend/app/api/routes/issues.py (new), backend/app/api/router.py (modified — registered issues router), backend/app/schemas/issues.py (modified — added BillingIssueCommentCreate, BillingIssueEditRequest), backend/tests/test_billing_issue_routes.py (new, 15 tests), backend/tests/test_billing_issue_permissions.py (new, 14 tests)
- verification: 29 new tests + full suite 43/43 passed against disposable docker-compose Postgres (torn down after); alembic heads/current unchanged at d6843df39f2f (no new migration needed); backend py_compile clean
- flags: auth/security/data impact (new authorization surface: Billing-reads/Finance-writes permission matrix enforced per docs/plans/2026-07-21-revenue-protection-issue-control.md Task 3) | decision made (reopen gated on an existing approved stage="test" `approvals` row rather than a separate flag; post_live_observation never reopenable, matching design's MVP scope) | no new risk beyond the existing pre-merge production alembic-stamp requirement

## 2026-07-21 11:33 — Task 4 of Revenue Protection Issue Control plan (server-side Move-to-Live gate)
- branch/commit: feature/entra-id-auth @ 1d153e2 (pushed to origin: no)
- files: backend/app/services/issue_control_service.py (new), backend/app/api/routes/approvals.py (modified — gate call inserted before the approval-webhook branch)
- verification: 9 new tests + full suite 52/52 passed against disposable docker-compose Postgres (torn down after); alembic heads/current unchanged at d6843df39f2f; backend py_compile clean. Confirmed no test ever exercises the real n8n webhook configured in backend/.env.local — blocked-path tests use finance_user but fail before the webhook branch runs, allowed-path tests use system_admin which skips that branch entirely.
- flags: auth/security/data impact (approval authorization logic changed: stage="test" + status="approved" now server-rejected while any finance_test_review issue is open, for every role) | decision made (gate applies to all roles, not just finance_user, per the "frontend-only hiding cannot bypass" acceptance criterion) | no new risk beyond the existing pre-merge production alembic-stamp requirement

## 2026-07-21 11:46 — Task 5 of Revenue Protection Issue Control plan (Finance review + Billing visibility UI)
- branch/commit: feature/entra-id-auth @ f53cd66 (pushed to origin: no)
- files: frontend/src/features/issues/{issue-api.js, issue-status.js, FinanceIssuePanel.jsx, IssueFormDialog.jsx, IssueActivityDialog.jsx} (new), frontend/src/components/ui/dialog.jsx (new), frontend/src/features/cycles/ApprovalStage.jsx (modified)
- verification: npm run lint + npm run build both clean; curl-based end-to-end check against a disposable docker-compose Postgres confirmed create/read/403/approval-gate all work through the new UI's exact API calls; dev servers booted cleanly. **Could not perform rendered-browser/console/responsive verification** (no chromium-cli/Playwright in this environment; port 5173 was already held by Gregory's own dev server, left untouched) — this is a real gap against the Frontend Design Quality Gate, flagged in the plan file
- flags: deployment-needed (frontend bundle changed) | decision made (built a local non-Radix Dialog primitive matching existing confirm-dialog.jsx convention rather than adding @radix-ui/react-dialog; issue data scoped to FinanceIssuePanel rather than AppDataContext) | new risk: **visual/responsive/console UI verification is outstanding** — recommend a manual browser pass (or approved Playwright install) before treating Task 5 as fully complete

Consolidated into a Hermes Update Pack on 2026-07-21 11:50 (3 entries above). Session handed off to a new session; see handover prompt for full context.

## 2026-07-21 05:15 — Finance cycle-review issue control planning package
- branch/commit: feature/entra-id-auth @ 3fa2156 (pushed to origin: no new commit)
- files: docs/FINANCE_ISSUE_CONTROL_DESIGN.md, docs/plans/2026-07-21-revenue-protection-issue-control.md, docs/wireframes/finance-test-review-approval.html
- verification: static wireframe served and browser-reviewed; git diff --check passed; independent Headroom/Claude review approved after two scope ambiguities were corrected
- flags: auth/security/data impact | deployment-needed | decision made | new risk

## 2026-07-21 05:20 — Implementation readiness and first-agent handoff pack
- branch/commit: feature/entra-id-auth @ 3fa2156 (pushed to origin: no new commit)
- files: docs/IMPLEMENTATION_READINESS_PACK.md, docs/FIRST_AGENT_PROMPT_REVENUE_PROTECTION.md
- verification: new-artifact whitespace checks passed after formatting correction; independent Headroom/Claude safety review approved
- flags: auth/security/data impact | deployment-needed | decision made | new risk

## 2026-07-21 06:05 — Pre-flight readiness assessment (Claude Code, read-only)
- branch/commit: feature/entra-id-auth @ 3fa2156 (pushed to origin: no)
- files: docs/IMPLEMENTATION_PRE_FLIGHT_REPORT.md (new, read-only report only)
- verification: CodeGraph unavailable (`codegraph: command not found`), fell back to direct git/grep/read inspection; no code run, no tests executed (none exist)
- flags: auth/security/data impact | decision made (Gate A/B/C all BLOCKED — NO-GO) | new risk (viewer role still authorized on nearly every GET endpoint and still seeded by default)

## 2026-07-21 09:58 — Phase 0 + Phase 1 of Revenue Protection Issue Control plan (Gates A and B closed locally)
- branch/commit: feature/entra-id-auth @ 29efa6f (pushed to origin: no)
- files: commit a823ad7 (bounded Entra integration slice, 74 files), commit 8f6d8db (viewer role retired: auth_service.py, init_db.py, all require_role() call sites, App.jsx, nav.js), commit 29efa6f (Alembic baseline: backend/alembic/, requirements.txt, ci.yml, docs/DEPLOYMENT_SAFETY.md, init_db.py comment)
- verification: backend py_compile clean; frontend npm run build + npm run lint clean; alembic heads/upgrade/downgrade/upgrade verified against disposable docker-compose Postgres (torn down after); alembic heads verified to work with no DATABASE_URL set (matches CI). Manual 3-role local-auth login smoke test NOT run (dev Postgres on :5432 unreachable this session) — open item for Gregory.
- flags: auth/security/data impact | deployment-needed (see below) | decision made (ENTRA_ENABLED stays false until Phase 9) | new risk: **production has NOT been `alembic stamp head`'d — merging this branch's CI change to main and deploying would run `alembic upgrade head` against a production DB that already has these tables via the old startup-mutation path, and CREATE TABLE would fail.** Documented prominently in docs/DEPLOYMENT_SAFETY.md; requires Gregory's direct production DB action before merge/deploy.

## 2026-07-21 10:04 — Phase 1a of Revenue Protection Issue Control plan (Gate C closed locally; all three gates now closed)
- branch/commit: feature/entra-id-auth @ 409951b (pushed to origin: no)
- files: backend/requirements-dev.txt (new), backend/tests/conftest.py (new), backend/tests/test_auth_roles.py (new, 6 tests), .github/workflows/ci.yml (postgres service + pytest step; fixed a deploy_metadata.txt path bug from the prior commit), .gitignore (recursive __pycache__/.pytest_cache ignore)
- verification: `pytest -q` run locally against disposable docker-compose Postgres — 6 passed, 0 failed (container torn down after). Includes a direct regression test proving a legacy role='viewer' row is now rejected at login (403 Unknown role).
- flags: auth/security/data impact | decision made (Gates A, B, C all closed locally — Phase 0/1/1a complete) | none new beyond the existing production-stamp risk logged above

Two items outstanding for Gregory (exact commands already given to him): (1) run `alembic stamp head` against production before this branch merges/deploys, (2) run the local 3-role login smoke test whenever his dev Postgres is reachable. Next: Phase 2 (issue persistence model + migration), which is the first task that touches actual business schema and should get its own review pass before proceeding.

## 2026-07-21 10:24 — Task 2 of Revenue Protection Issue Control plan (issue persistence model + migration)
- branch/commit: feature/entra-id-auth @ 9fec22f (pushed to origin: no)
- files: backend/app/models/billing_issue.py, billing_issue_activity.py (new), backend/app/models/__init__.py (modified), backend/alembic/versions/d6843df39f2f_add_billing_issue_controls.py (new), backend/app/schemas/issues.py (new), backend/tests/test_billing_issues_model.py (new, 8 tests), backend/tests/conftest.py (modified — new db_session fixture, client fixture now runs real Alembic migrations instead of create_all())
- verification: full backend suite 14/14 passed; alembic heads/upgrade/downgrade/upgrade verified against disposable docker-compose Postgres (torn down after); alembic heads verified without DATABASE_URL set
- flags: auth/security/data impact (none — no auth surface touched) | data/schema impact (three new tables + seed data, additive only, no production touched) | decision made (classifications shared via context="finance_review" for both finance_test_review and post_live_observation issues, per design doc section 4) | new risk: none beyond the existing pre-merge production-stamp requirement

Note: DB-level CHECK constraints now enforce completion-requires-outcome/actor/time and raised_in_error-requires-comment directly at the schema layer (belt-and-suspenders ahead of Task 3's API-layer enforcement). The "Other classification requires detail" rule needs a DB lookup of the classification name and is deferred to Task 3 (route/service layer), where classification_id can actually be resolved. Next: Task 3 (authorization + issue API lifecycle) — the first task exposing new endpoints.

Consolidated into a Hermes Update Pack on 2026-07-21 10:30 (7 entries above). Tasks 3, 4, and 5 (originally logged here) were consolidated into a second Hermes Update Pack on 2026-07-21 11:50 and moved under the "Flushed" heading at the top of this file.
